# --- Core Framework (Mobile UI) ---
kivy>=2.3.0
# kivymd>=1.1.1  # (Optional: เปิดใช้งานถ้าคุณใช้ Material Design widgets)

# --- AetherBus & Networking (The Nervous System) ---
# ใช้สำหรับเชื่อมต่อกับ Backend หรือทำ Ping/Pong Keep-alive
websockets>=12.0
httpx>=0.27.0

# --- Local Kernel Runtime (The Edge Brain) ---
# เรายังคงรัน FastAPI ได้บนมือถือเพื่อเป็น Local Server (Edge Node)
fastapi>=0.109.0
uvicorn>=0.27.0
# หมายเหตุ: ตัด uvloop ออก เพื่อใช้ asyncio มาตรฐานของ Android แทน

# --- Data & Configuration ---
pydantic>=2.6.0
python-dotenv>=1.0.0

# --- Native Features (Mobile Specific) ---
# ไลบรารีสำหรับเรียกใช้ฟีเจอร์มือถือ (สั่น, แจ้งเตือน, GPS)
plyer
# jnius  # (Buildozer มักจะลงให้อัตโนมัติ แต่ใส่ไว้กันเหนียวได้)

# --- Utilities (Lightweight) ---
typing-extensions>=4.9.0
idna>=3.6
